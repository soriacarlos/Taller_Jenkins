pipeline {
  agent any

  tools {
    nodejs "node"
  }

  parameters {
    string(name: 'container_name', defautValue: 'pagina_web', description: 'Nombre del contenedor de Docker.')
    string(name: 'image_name', defautValue: 'pagina_img', description: 'Nombre de la imagen de Docker.')
    string(name: 'tag_image', defautValue: 'lts', description: 'Tag de la imagen de la página')
    string(name: 'container_port', defautValue: 80, description: 'Puerto del contenedor')
  }

  stages {
    stage('install') {
      steps {
        git branch: 'develop', url: 'https://github.com/soriacarlos/Taller_Jenkins.git'
        dir('frontend') {
          sh 'npm install'
        }
      }
    }

    stage('test') {
      steps {
        dir('frontend') {
          sh 'npm run test'
        }
      }
    }

  }
}
